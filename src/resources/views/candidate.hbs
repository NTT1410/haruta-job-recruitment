<div class="row">
	<h3 class="text-muted fw-bold fs-5 pb-4">

		Candidates
	</h3>
</div>

<div class="row candidate_1">
	<div class="c-candi">
		<a href="/cdd/create">
			<button type="button" class="btn btn-primary">ADD</button>
		</a>
		<button class="btn btn-danger" id="button-delete">Delete</button>
		<button class="btn btn-success" id="button-edit">Edit</button>
	</div>
	{{!-- <table class="table align-middle mb-0 mt-3 bg-white table-hover">
		<thead class="bg-light">
			<tr>
				<th>#</th>
				<th>Name</th>
				<th>Phone</th>
				<th>Created at</th>
				<th>Status</th>
				<th colspan="2">Actions</th>
			</tr>
		</thead>
		<tbody class="tbody">
			{{#each users}}
				<tr>
					<td scope="row">{{sum @index 1}}</td>
					<td>
						<div class="d-flex align-items-center">
							<img
								src={{this.avatar}}
								alt=""
								style="width: 45px; height: 45px"
								class="rounded-circle"
							/>
							<div class="ms-3">
								<p class="fw-bold mb-1 td-fullname">{{this.first_name}}
									{{this.last_name}}</p>
								<p class="text-muted mb-0">{{this.email}}</p>
							</div>
						</div>
					</td>
					<td>
						<p class="text-muted mb-0">{{this.phone}}</p>
					</td>
					<td>
						<p class="text-muted mb-0">{{this.createdAt}}</p>
					</td>
					<td>
						{{#if active}}
							<span class="badge badge-success rounded-pill d-inline">
								active
							</span>
						{{else}}
							<span class="badge badge-danger rounded-pill d-inline">
								Inactive
							</span>
						{{/if}}
					</td>
					<td>
						<button type="button" class="btn btn-link btn-sm btn-rounded">
							<a href="/candidate/detail/{{this._id}}">Edit</a>
						</button>
						<button type="button" class="btn btn-link btn-sm btn-rounded">
							<a href="/candidate/detail/{{this._id}}">Delete</a>
						</button>
					</td>
				</tr>
			{{/each}}
		</tbody>
	</table> --}}
</div>
<div id="table-candidate">
	<table id="example" class="display" width="100%">
		<thead>
			<tr>
				<th>Name</th>
				<th>Phone</th>
				<th>Created At</th>
				<th>Status</th>
			</tr>
		</thead>
	</table>
</div>
<div class="mt-3 mb-5" id="paging"></div>